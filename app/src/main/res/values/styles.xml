<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.EVLogbook" parent="android:Theme.Light">
        <!-- Primary brand color -->
        <item name="android:colorPrimary">#2563EB</item>
        <item name="android:colorPrimaryDark">#1D4ED8</item>

        <!-- Background and text colors -->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:colorBackground">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">#666666</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">#2563EB</item>
    </style>

    <!-- Dark theme variant -->
    <style name="Theme.EVLogbook.Dark" parent="android:Theme.Black">
        <item name="android:colorPrimary">#3B82F6</item>
        <item name="android:colorPrimaryDark">#2563EB</item>

        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">#CCCCCC</item>

        <item name="android:statusBarColor">#1F2937</item>
    </style>

    <!-- No action bar theme for activities -->
    <style name="Theme.EVLogbook.NoActionBar" parent="android:Theme.Light.NoTitleBar">
        <item name="android:colorPrimary">#2563EB</item>
        <item name="android:colorPrimaryDark">#1D4ED8</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
